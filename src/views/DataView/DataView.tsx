import React from "react";
import styles from "./dataView.module.scss";

interface Props {}

const DataView = (props: Props) => {
  return <div className={styles.dataView}>This is pretty data table</div>;
};

export default DataView;
